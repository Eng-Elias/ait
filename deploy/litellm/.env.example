# ============================================================
# LiteLLM Proxy - Environment Configuration
# ============================================================
# Copy this file to .env and fill in the values before deploying.
#
#   cp .env.example .env
#
# NEVER commit .env to version control.
# ============================================================

# --- Required ---------------------------------------------------

# Master key for LiteLLM admin dashboard and virtual key management.
# Generate a strong random key, e.g.:  openssl rand -hex 32
LITELLM_MASTER_KEY=sk-change-me-to-a-strong-random-key

# HuggingFace API token for accessing model endpoints.
# Create one at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# HuggingFace Dedicated Endpoint URL (root path, no /v1/).
# Example: https://xxxxx.us-east-1.aws.endpoints.huggingface.cloud
HF_ENDPOINT_URL=https://your-endpoint.endpoints.huggingface.cloud

# --- Database (required for virtual keys & spend tracking) ------

# PostgreSQL connection string.
# Supabase example: postgresql://postgres:<password>@db.<project>.supabase.co:5432/postgres
# Local Docker example: postgresql://litellm:litellm@db:5432/litellm
DATABASE_URL=postgresql://litellm:litellm@db:5432/litellm

# --- Optional ---------------------------------------------------

# LiteLLM UI admin password (for the web dashboard).
UI_PASSWORD=admin

# Port the proxy listens on inside the container.
LITELLM_PORT=4000

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
